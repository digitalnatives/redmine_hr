<%= t('hr_mailer.withdrawn.text',{
  name: @request.hr_employee_profile.user.name,
  type: t("hr.holiday_request.#{@request.request_type}"),
  days: @request.days,
  start: @request.start_date,
  link: hr_index_url(anchor: "holiday_requests/#{@request.id}")
}).html_safe %>
